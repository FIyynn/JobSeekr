Return exactly one JSON object.

Use the task goal and digitized user profile to choose:
- the search context
- a small set of role-based query groups

Do not emit command blocks.
Do not emit markdown.
Do not invent tool steps.
Do not choose caps.
